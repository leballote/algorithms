//TODO: finish this, find a better solution
export default function searchInMatrix(matrix: number[][]) {
  const n = matrix.length;
  const m = matrix[0]?.length ?? 0;
  let mx = Math.floor(n / 2);
}

function aux(matrix: number[][], i: number, j: number) {}

const mat = [
  [10, 20, 30, 40],
  [15, 25, 35, 45],
  [27, 29, 37, 48],
  [32, 33, 39, 50],
];
